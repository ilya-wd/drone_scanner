import { Navbar } from 'react-bootstrap'

const Header = (drones) => {
  const xero = 0

  const totalDrones = drones.length

  return (
    <Navbar>
      <div>
        <span></span>
      </div>
    </Navbar>
  )
}
